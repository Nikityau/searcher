import {TMedia} from "../types";

export const useUnsplash = () => {

    const searchImage = async (query): Promise<Array<TMedia> | null> => {
        return null
    }



    return {
        searchImage
    }
}